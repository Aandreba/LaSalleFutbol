/**
 * Enum class containing types of actions that could have had happened at a game.
 * author: Gerard Marquina Rubio
 */
public enum ActionType {
    GOAL,
    FOUL,
    OUT,
    CORNER,
    OFFSIDE,
    YELLOW_CARD,
    RED_CARD,
    OTHER
}
